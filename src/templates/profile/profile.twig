<component is="profile"
           :selectopen-select="{{ sort_type|json_encode|e('html_attr') }}"
           :pre-link="{{ pre_link|json_encode|e('html_attr') }}"
           :user="{{ user|json_encode()|e('html_attr') }}"
></component>
