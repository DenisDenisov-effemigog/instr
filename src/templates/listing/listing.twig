<div class="listing">
    <div class="listing__header">
        <h1 class="listing__title">{{ listingCategoryTitle }}</h1>
        <div class="items-cont">12 210 товаров</div>
    </div>
    <div class="listing__shields">
        {% for shield in shields %}
            <div class="listing__shield">
                {{ shield }}
            </div>
        {% endfor %}
    </div>
    <div class="listing__content">
        <div class="listing__sidebar">
            {% if filters is not empty %}
                <div class="listing__categories">
                    <div class="listing__categories__title">{{ listingCategoryTitle }}</div>{#TODO оформить тайтл#}
                    <component is="further-subcategories"
                               :categories="{{ listingCategories.subcategories|json_encode()|e('html_attr') }}"
                               :class-name="'listing'"
                    ></component>{#TODO вынести из компоненты#}
                    {#{% for category in categories.subcategories %}
                        <a href="{{ category.url }}" class="listing__categories_sabcategory">{{ category.title }}</a>
                    {% endfor %}#}
                </div>
                <div class="listing__sidebar_filters">  
                    {% include './filter.twig' with {'filters': filters} %}
                </div>
            {% else %}
                <component is="catalogue-subcategories"
                           :categories="{{ listingCategories.subcategories|json_encode()|e('html_attr') }}"
                           :class-name="'listing'"
                ></component>{#TODO вынести из компоненты#}
            {% endif %}
        </div>
        <div class="listing__products">

            <component is="sticky-listing-actions"
                :filters="{{ filters|json_encode|e('html_attr') }}"
                :points="{{ sort_types|json_encode|e('html_attr') }}"
                :selectopen-select="{{ sort_type|json_encode|e('html_attr') }}"
            ></component>

            <div class="listing__grid">
                {% for product in products %}
                    <div class="listing__card">
                        <card
                            :change-icon="false"
                            :card-grid="'grid'"
                            :product="{{ product|json_encode()|e('html_attr') }}"
                        >
                            {% include '../partials/card.twig' with {'product': product} %}
                        </card>
                    </div>
                {% endfor %}
            </div>
            <div class="listing__grid listing__grid--horiz">
                {% for product in products %}
                    <div class="listing__card">
                        <card
                            :change-icon="false"
                            :card-grid="'grid'"
                            :product="{{ product|json_encode()|e('html_attr') }}"
                        >
                            {% include '../partials/card.twig' with {'product': product} %}
                        </card>
                    </div>
                {% endfor %}
            </div>
            <div class="listing__pagination">
                {% include './listing-pagination/listing-pagination-btn.twig' %}
                {% include './listing-pagination/listing-pagination.twig' %}
            </div>
        </div>
    </div>
</div>