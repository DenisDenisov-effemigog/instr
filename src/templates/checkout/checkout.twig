<component is="checkout"
           :deliveries="{{ deliveries|json_encode|e('html_attr') }}"
           :user="{{ user|json_encode|e('html_attr') }}"
           :payments="{{ payments|json_encode|e('html_attr') }}"
           :sale-price="{{ salePrice|json_encode|e('html_attr') }}"
           :delivery-points="{{ deliveryPoints|json_encode|e('html_attr') }}"
           :cities="{{ cities|json_encode|e('html_attr') }}"
           :current-city="{{ currentCity|json_encode|e('html_attr') }}"
           :streets="{{ streets|json_encode|e('html_attr') }}"
           :conditions-link="{{ conditionsLink|json_encode|e('html_attr') }}"
           :delivery-link="{{ deliveryLink|json_encode|e('html_attr') }}"
           :phone-mask="{{ phone_mask|json_encode|e('html_attr') }}"
           :code-mask="{{ code_mask|json_encode|e('html_attr') }}"
></component>