<div class="cart">
    <div class="container">
        <component is="cart" 
                   :view="{{ view_type|json_encode|e('html_attr') }}"
                   :discounts="{{ discounts|json_encode|e('html_attr') }}"
                   :sale-price="{{ salePrice|json_encode|e('html_attr') }}"
        ></component>
    </div>
</div>