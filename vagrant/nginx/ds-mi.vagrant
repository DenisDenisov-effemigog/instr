server
{
    listen 80;
    server_name ds-mi.vagrant;

    access_log off;
    error_log /var/log/nginx/ds-mi.error.log;

    root /var/www/ds-mi/public;
    index index.html index.htm;
    
    location ~ ^/my/$ {
        rewrite ^/my/(.*)$ /pages_03_profile.html;
    }
    
    location ~ ^/my/(.+\.(?:js|css|png|jpg|ico|woff|eot|woff2|svg))$ {        
        rewrite ^/my/(.*)$ /$1 break;
    }
}